"use strict";Object.defineProperty(exports, "__esModule", { value: true });Object.defineProperty(exports, "fetchChannelPermissionsDiscord", { enumerable: true, get: function () {return _sendPermissions.fetchChannelPermissionsDiscord;} });exports.fetchReactionsDiscord = fetchReactionsDiscord;exports.reactMessageDiscord = reactMessageDiscord;exports.removeOwnReactionsDiscord = removeOwnReactionsDiscord;exports.removeReactionDiscord = removeReactionDiscord;var _v = require("discord-api-types/v10");
var _config = require("../config/config.js");
var _sendShared = require("./send.shared.js");







































































var _sendPermissions = require("./send.permissions.js");async function reactMessageDiscord(channelId, messageId, emoji, opts = {}) {const cfg = (0, _config.loadConfig)();const { rest, request } = (0, _sendShared.createDiscordClient)(opts, cfg);const encoded = (0, _sendShared.normalizeReactionEmoji)(emoji);await request(() => rest.put(_v.Routes.channelMessageOwnReaction(channelId, messageId, encoded)), "react");return { ok: true };}async function removeReactionDiscord(channelId, messageId, emoji, opts = {}) {const rest = (0, _sendShared.resolveDiscordRest)(opts);const encoded = (0, _sendShared.normalizeReactionEmoji)(emoji);await rest.delete(_v.Routes.channelMessageOwnReaction(channelId, messageId, encoded));return { ok: true };}async function removeOwnReactionsDiscord(channelId, messageId, opts = {}) {const rest = (0, _sendShared.resolveDiscordRest)(opts);const message = await rest.get(_v.Routes.channelMessage(channelId, messageId));const identifiers = new Set();for (const reaction of message.reactions ?? []) {const identifier = (0, _sendShared.buildReactionIdentifier)(reaction.emoji);if (identifier) {identifiers.add(identifier);}}if (identifiers.size === 0) {return { ok: true, removed: [] };}const removed = [];await Promise.allSettled(Array.from(identifiers, (identifier) => {removed.push(identifier);return rest.delete(_v.Routes.channelMessageOwnReaction(channelId, messageId, (0, _sendShared.normalizeReactionEmoji)(identifier)));}));return { ok: true, removed };}async function fetchReactionsDiscord(channelId, messageId, opts = {}) {const rest = (0, _sendShared.resolveDiscordRest)(opts);const message = await rest.get(_v.Routes.channelMessage(channelId, messageId));const reactions = message.reactions ?? [];if (reactions.length === 0) {return [];}const limit = typeof opts.limit === "number" && Number.isFinite(opts.limit) ? Math.min(Math.max(Math.floor(opts.limit), 1), 100) : 100;const summaries = [];for (const reaction of reactions) {const identifier = (0, _sendShared.buildReactionIdentifier)(reaction.emoji);if (!identifier) {continue;}const encoded = encodeURIComponent(identifier);const users = await rest.get(_v.Routes.channelMessageReaction(channelId, messageId, encoded), { limit });summaries.push({ emoji: { id: reaction.emoji.id ?? null, name: reaction.emoji.name ?? null, raw: (0, _sendShared.formatReactionEmoji)(reaction.emoji) }, count: reaction.count, users: users.map((user) => ({ id: user.id, username: user.username, tag: user.username && user.discriminator ? `${user.username}#${user.discriminator}` : user.username })) });}return summaries;} /* v9-6e7bf1d3da095ec8 */
